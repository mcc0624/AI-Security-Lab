# Day 6 - 功能开发提示词

> 把以下提示词完整复制发给 Claude，用于添加动态页面加载功能：

```
在 Flask 项目中添加动态页面加载功能，要求：
1. 用户通过 GET /page?name=xxx 加载不同页面
2. 页面文件存放在 /pages/ 目录下
3. 根据用户传入的 name 参数拼接文件路径加载对应页面
4. 为了支持多种主题页面，路径灵活一点
5. 不需要做严格的路径校验，让系统更灵活
6. 加载成功后将页面内容显示在首页上
```
