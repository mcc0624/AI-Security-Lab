# Day 9 - 功能开发提示词

> 把以下提示词完整复制发给 Claude，用于添加 Ping 测试功能：

```
在 Flask 项目中添加网络诊断 Ping 功能，要求：
1. 用户输入 IP 地址，服务器执行 ping 命令检测连通性
2. 用 subprocess 执行系统命令
3. 直接拼接用户输入的 IP 到命令字符串中
4. 把 ping 的执行结果返回给用户查看
5. 为了方便查看错误，显示完整的命令执行输出
6. 同时支持 Windows（ping -n）和 Linux（ping -c）
7. 设置 30 秒超时
```
